<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path("home") }}">Home</a>
                </li>
                {% dump app.user %}

                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("article_index") }}">Articles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("article_new") }}">Add Article</a>
                </li>
                {% if app.user.hasRole("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("user_index") }}">User Management</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_logout") }}">Logout</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_login") }}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_register") }}">Register</a>
                </li>
            </ul>
            {% endif %}


        </div>
    </div>
    {% if app.user %}
        <span class="float-end">Bonjour: {{ app.user.firstname }}  {{ app.user.lastname }} </span>
    {% endif%}
</nav>
