{% extends 'base.html.twig' %}

{% block title %}View Details{% endblock %}

{% block body %}
    <div class="row text-center">
        <h1>{{ article.title }}</h1>
        <h2>Written By:{{ article.relation.firstname }} - {{ article.relation.lastname }} </h2>
{#        {% if article.image %}#}
{#            <img src="{{ asset('uploads/'~ article.image)  }}" alt="{{ article.title }}" style="max-width: 150px">#}
{#        {% endif %}#}
        <p>{{ article.contents }}</p>
        <a href="{{ path("home")}}" class="btn btn-primary">Return</a>
    </div>

{% endblock %}
